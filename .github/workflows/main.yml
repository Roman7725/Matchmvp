name: Build Release AAB

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        distribution: 'zulu'
        java-version: '17'
        cache: 'gradle'

    # Переходим в папку MatchMVP и даем права файлу gradlew
    - name: Grant execute permission for gradlew
      run: |
        cd MatchMVP
        chmod +x gradlew

    # Декодируем секрет и сохраняем его в MatchMVP/app/release.jks
    - name: Decode Keystore
      run: |
        echo "${{ secrets.KEYSTORE_BASE64 }}" | base64 --decode > MatchMVP/app/release.jks

    # Запускаем сборку прямо из папки MatchMVP
    - name: Build Release Bundle (AAB)
      run: |
        cd MatchMVP
        ./gradlew bundleRelease
      env:
        KEYSTORE_PASSWORD: ${{ secrets.KEYSTORE_PASSWORD }}
        KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}
        KEY_ALIAS: ${{ secrets.KEY_ALIAS }}

    # Вытаскиваем готовый AAB из папки MatchMVP
    - name: Upload AAB Artifact
      uses: actions/upload-artifact@v4
      with:
        name: release-aab
        path: MatchMVP/app/build/outputs/bundle/release/app-release.aab
